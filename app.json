{
  "addons": [
    "heroku-redis:hobby-dev"
  ],
  "buildpacks": [],
  "env": {
    "MAIL_FROM": {
      "required": true
    },
    "SEED_ADMIN_EMAIL": {
      "required": true
    },
    "SEED_ADMIN_INITIAL_PASSWORD": {
      "required": true
    }
  },
  "formation": {},
  "name": "abtion-secret",
  "scripts": {},
  "stack": "heroku-18",
}
